# ULW Loop Bootstrap Notepad

Task: Execute `.omo/plans/20260610-inference-optimization-harness.md` until verification and completion.

Selected skills:
- omo:ulw-loop — explicit user invocation; durable evidence-led execution loop.
- omo:programming — Python implementation/tests are required; TDD and strict toolchain expectations apply.
- omo:git-master — repository requires milestone commits; use atomic Lore-style commits, with signing fallback noted if needed.
- omo:review-work / codex-ultrawork-reviewer — final implementation is multi-file/cross-cutting and requires rigorous review gate.
- best-practice-research/researcher — only if live/current LM Studio docs become implementation-critical; current plan already captured official URLs.

Scope sizing:
- Distinct surfaces: CLI harness, state/event artifacts, report generation, LM Studio adapter, correctness gate planner, worklog/audit, tests, tmux QA, git commits.
- Expected files: 5-8 Python files under `scripts/`, 5+ tests under `tests/`, `worklog.md`, artifacts under `.omo/ulw-loop/evidence` and `artifacts/optimization`.
- Non-trivial: yes; planner agent Hegel spawned for wave ordering.

Manual QA channel default: tmux transcripts under `.omo/ulw-loop/evidence/`.
